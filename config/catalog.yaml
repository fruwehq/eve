# v2 catalog
# Locations are now contributed by provider manifests (each provider declares
# its per-location data in its catalog.locations block). This file holds
# only the version marker.
version: 1
