#!/usr/bin/env ruby
# frozen_string_literal: true

instance = ENV["EGAME_INSTANCE_NAME"] || ARGV.first
unless instance && !instance.empty?
  warn "Usage: commands/down <instance>"
  exit 2
end

warn "[down] instance=#{instance} uses metal hardware; nothing to destroy."
