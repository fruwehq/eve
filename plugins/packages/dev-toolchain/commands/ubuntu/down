#!/usr/bin/env sh
set -eu

profile="${EGAME_INSTANCE_NAME:?EGAME_INSTANCE_NAME is required}"

./scripts/instance-ssh "$profile" -- \
  'rustup self uninstall -y || true; sudo apt-get remove -y build-essential pkg-config libssl-dev || true'
