#!/usr/bin/env sh
set -eu

profile="${EGAME_INSTANCE_NAME:?EGAME_INSTANCE_NAME is required}"

./scripts/instance-ssh "$profile" -- \
  'sudo systemctl disable --now tigervncserver@:1 || true; sudo apt-get remove -y tigervnc-standalone-server tigervnc-common || true'
