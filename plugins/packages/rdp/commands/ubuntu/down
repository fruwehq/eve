#!/usr/bin/env sh
set -eu

profile="${EGAME_INSTANCE_NAME:?EGAME_INSTANCE_NAME is required}"

./scripts/instance-ssh "$profile" -- \
  'sudo systemctl disable --now xrdp || true; sudo apt-get remove -y xrdp || true'
