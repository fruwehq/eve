#!/usr/bin/env sh
set -eu

profile="${EGAME_INSTANCE_NAME:?EGAME_INSTANCE_NAME is required}"

./scripts/instance-ssh "$profile" -- \
  'systemctl --user disable --now sunshine || true; sudo systemctl disable --now sunshine || true; sudo apt-get remove -y sunshine || true'
