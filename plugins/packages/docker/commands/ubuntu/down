#!/usr/bin/env sh
set -eu

exec ./scripts/package-down-command docker 'sudo systemctl disable --now docker || true; sudo apt-get remove -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin || true'
