{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ephemeral.dev/schemas/plugin-manifest.schema.json",
  "title": "Plugin Manifest",
  "description": "Schema for eve-plugin.yaml manifests covering both provider and package plugin kinds.",

  "type": "object",
  "required": ["api_version", "kind", "id", "display_name", "commands", "supports"],
  "additionalProperties": false,

  "properties": {
    "api_version": {
      "description": "API version. Only eve.plugin/v1 is accepted; unknown versions must be refused.",
      "type": "string",
      "const": "eve.plugin/v1"
    },

    "kind": {
      "description": "Plugin kind determines the required commands and allowed fields.",
      "type": "string",
      "enum": ["provider", "package"]
    },

    "id": {
      "description": "Plugin identifier. Must match [a-z][a-z0-9-]*.",
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$"
    },

    "display_name": {
      "description": "Human-readable plugin name shown in the TUI and CLI listings.",
      "type": "string",
      "minLength": 1
    },

    "commands": {
      "description": "Map of command name to execution spec. Required commands differ by kind.",
      "type": "object",
      "additionalProperties": { "$ref": "#/$defs/command_spec" }
    },

    "supports": {
      "description": "Compatibility constraints. Shape differs by kind.",
      "type": "object",
      "additionalProperties": true
    },

    "env": {
      "description": "List of environment variable names this plugin reads.",
      "type": "array",
      "items": { "type": "string" }
    },

    "actions": {
      "description": "UI actions exposed by provider and package plugins.",
      "type": "array",
      "items": { "$ref": "#/$defs/action" }
    },

    "capabilities": {
      "description": "Behavioral flags the UI/core can consult. Distinct from supports (compatibility) and actions (user-clickable).",
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["host-ssh", "login", "needs-provider-ip", "password"]
      }
    },

    "compatibility": {
      "description": "Compatibility matrix listing supported platform/desktop/session combinations (package plugins only).",
      "type": "array",
      "items": { "$ref": "#/$defs/compatibility_entry" }
    },

    "compatibility_enforced": {
      "description": "Whether compatibility failures are hard errors during dispatch (package plugins only).",
      "type": "boolean"
    },

    "access": {
      "description": "Provider-owned VM access identity rules used to populate resolved access.bootstrap_user, provision_user, and human_user.",
      "type": "object",
      "additionalProperties": { "$ref": "#/$defs/access_rules" }
    },

    "conflicts_with": {
      "description": "Package ids that cannot coexist with this package (package plugins only).",
      "type": "array",
      "items": { "type": "string" }
    },

    "config_schema": {
      "description": "Config and secret field declarations. Valid on both provider and package manifests. On packages, values are namespaced under the package id (e.g. mypkg.my_field).",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "config": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/field_spec" }
        },
        "secrets": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/field_spec" }
        }
      }
    },

    "down": {
      "description": "Package down behavior metadata (package plugins only).",
      "type": "object",
      "properties": {
        "destructive": {
          "type": "boolean",
          "description": "Whether down requires explicit confirmation."
        }
      },
      "additionalProperties": false
    },

    "install": {
      "description": "Install metadata per OS family, consumed by provisioning scripts (package plugins only).",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "ubuntu": { "$ref": "#/$defs/install_os_spec" },
        "windows": { "$ref": "#/$defs/windows_install_spec" }
      }
    },

    "depends_on": {
      "description": "Package IDs this plugin requires. Used by the resolver to validate that dependencies are selected and to order install steps so dependencies run first.",
      "type": "array",
      "items": { "type": "string" },
      "uniqueItems": true
    },

    "catalog": {
      "description": "Provider-contributed catalog rows (machines, provider-specific OS image fields, inits). Only meaningful on provider manifests. The aggregator merges these onto shared OS identity rows to produce the effective catalog.",
      "type": "object",
      "additionalProperties": true
    },

    "bundles": {
      "description": "Bundle definitions contributed by this package manifest. Each bundle composes a set of included package ids. Only meaningful on package manifests.",
      "type": "array"
    },

    "artifacts": {
      "description": "Declared build outputs for this package. Phase 0: declaration only — no execution wiring.",
      "type": "array"
    },

    "requires": {
      "description": "Compatibility requirements. The eve range is enforced at load as a core gate (reject if the running core version is outside the range). The plugins map is validated for shape only; full graph resolution is deferred to Phase 1.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "eve": {
          "type": "string",
          "description": "Semver range string for the required eve core version (e.g. \">=4.0,<5.0\" or \"^4.0\")."
        },
        "plugins": {
          "type": "object",
          "description": "Map of plugin id to required semver range string.",
          "additionalProperties": { "type": "string" }
        }
      }
    }
  },

  "allOf": [
    {
      "if": { "properties": { "kind": { "const": "provider" } } },
      "then": {
        "required": ["access", "env"],
        "properties": {
          "commands": {
            "required": ["resolve", "init", "plan", "up", "down", "start", "stop", "status", "ip", "ssh", "validate"]
          },
          "supports": { "$ref": "#/$defs/provider_supports" },
          "catalog": { "$ref": "#/$defs/catalog_contribution" }
        }
      }
    },
    {
      "if": { "properties": { "kind": { "const": "package" } } },
      "then": {
        "required": ["env"],
        "properties": {
          "commands": {
            "required": ["install", "status", "down"]
          },
          "supports": { "$ref": "#/$defs/package_supports" },
          "bundles": {
            "type": "array",
            "items": { "$ref": "#/$defs/bundle_entry" }
          },
          "artifacts": {
            "type": "array",
            "items": { "$ref": "#/$defs/artifact_entry" }
          }
        }
      }
    }
  ],

  "$defs": {
    "command_spec": {
      "description": "Execution spec for a single plugin command.",
      "type": "object",
      "required": ["exec"],
      "additionalProperties": false,
      "properties": {
        "exec": {
          "type": "string",
          "description": "Path to the executable, relative to the plugin directory or the project root."
        },
        "args": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Arguments passed to the executable."
        }
      }
    },

    "access_rules": {
      "description": "Access identity rules for one OS family.",
      "type": "object",
      "required": ["bootstrap_user", "provision_user", "human_user"],
      "additionalProperties": false,
      "properties": {
        "bootstrap_user": { "$ref": "#/$defs/access_user_rule" },
        "provision_user": { "$ref": "#/$defs/access_user_rule" },
        "human_user": { "$ref": "#/$defs/access_user_rule" }
      }
    },

    "access_user_rule": {
      "description": "Rule for resolving one user identity. env reads an environment variable, value is a constant, location reads a provider location field, and fallback references another access field.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": false,
      "properties": {
        "env": { "type": "string" },
        "fallback": {
          "type": "string",
          "enum": ["bootstrap_user", "provision_user", "human_user"]
        },
        "location": { "type": "string" },
        "value": { "type": "string" }
      }
    },

    "provider_supports": {
      "description": "Provider plugin compatibility constraints.",
      "type": "object",
      "properties": {
        "engines": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["terraform", "vagrant", "qemu", "metal"]
          }
        },
        "kinds": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["vm", "metal"]
          }
        }
      },
      "additionalProperties": false
    },

    "package_supports": {
      "description": "Package plugin compatibility constraints. Contract rule: the same package id may exist in multiple sources iff their supports are disjoint (e.g. one for ubuntu, one for windows). Overlapping supports for one id is a conflict and requires explicit EVE_PLUGIN_ALLOW_OVERRIDE. Enforcement is deferred to the Phase 1/3 resolver; today load_all already rejects duplicate kind:id pairs without that override.",
      "type": "object",
      "properties": {
        "os_families": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["ubuntu", "windows"]
          }
        },
        "arches": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["amd64", "arm64"]
          }
        },
        "os_ids": {
          "type": "array",
          "items": { "type": "string" }
        },
        "os_versions": {
          "type": "array",
          "items": { "type": "string" }
        },
        "ubuntu_versions": {
          "type": "array",
          "items": { "type": "string" }
        },
        "windows_versions": {
          "type": "array",
          "items": { "type": "string" }
        }
      },
      "additionalProperties": false
    },

    "action": {
      "description": "A UI action exposed by the plugin.",
      "type": "object",
      "required": ["id", "label", "target"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9-]*$",
          "description": "Action identifier."
        },
        "label": {
          "type": "string",
          "minLength": 1,
          "description": "Button label shown in the TUI."
        },
        "target": {
          "type": "string",
          "minLength": 1,
          "description": "Action target dispatched by package-action, provider dispatch, or a compatibility Make target."
        },
        "destructive": {
          "type": "boolean",
          "description": "Whether the UI must confirm before running this action."
        },
        "expected_output": {
          "type": "string",
          "description": "Expected output shape for callers that need to parse the action result."
        },
        "help": {
          "type": "string",
          "description": "Short operator-facing help text for the action."
        },
        "interactive": {
          "type": "boolean",
          "description": "Whether this action needs an attached terminal/browser-capable interactive session."
        },
        "native": {
          "type": "boolean",
          "description": "Whether this action uses a native protocol handler."
        },
        "prompt": { "$ref": "#/$defs/action_prompt" },
        "requires": {
          "type": "object",
          "description": "Optional state predicates that control when this action is available.",
          "additionalProperties": true
        }
      }
    },

    "action_prompt": {
      "description": "A user-choice prompt rendered before the action fires.",
      "type": "object",
      "required": ["title", "message", "env", "choices"],
      "additionalProperties": false,
      "properties": {
        "title": {
          "type": "string",
          "minLength": 1
        },
        "message": {
          "type": "string",
          "minLength": 1
        },
        "env": {
          "type": "string",
          "pattern": "^[A-Z][A-Z0-9_]*$",
          "description": "Environment variable name for the selected choice."
        },
        "choices": {
          "type": "array",
          "minItems": 1,
          "items": { "$ref": "#/$defs/prompt_choice" }
        }
      }
    },

    "prompt_choice": {
      "description": "A single choice in an action prompt.",
      "type": "object",
      "required": ["value", "label"],
      "additionalProperties": false,
      "properties": {
        "value": {
          "type": "string",
          "minLength": 1
        },
        "label": {
          "type": "string",
          "minLength": 1
        },
        "variant": {
          "type": "string",
          "enum": ["default", "primary", "success", "warning", "error"]
        }
      }
    },

    "compatibility_entry": {
      "description": "One row in the package compatibility matrix.",
      "type": "object",
      "required": ["platform", "desktop", "session", "status", "notes"],
      "additionalProperties": false,
      "properties": {
        "platform": {
          "type": "string",
          "minLength": 1
        },
        "desktop": {
          "type": "string",
          "minLength": 1
        },
        "session": {
          "type": "string",
          "minLength": 1
        },
        "status": {
          "type": "string",
          "enum": ["supported", "wip", "unsupported", "legacy"]
        },
        "notes": {
          "type": "string",
          "minLength": 1
        }
      }
    },

    "install_os_spec": {
      "description": "Ubuntu install step specification.",
      "type": "object",
      "required": ["steps"],
      "additionalProperties": false,
      "properties": {
        "steps": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 1
        },
        "package_markers": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },

    "field_spec": {
      "description": "A single config or secret field declared by a provider manifest.",
      "type": "object",
      "additionalProperties": false,
      "required": ["type"],
      "properties": {
        "type": {
          "enum": ["string", "integer", "boolean", "path", "url", "host", "enum"]
        },
        "enum": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 1
        },
        "default": {},
        "description": { "type": "string" },
        "required": { "type": "boolean" },
        "env_var": {
          "description": "Environment variable name(s) CoreEmit for this field. For config fields, ConfigEnv emits the value during config-env emission (make/shell/json). For secrets, provider dispatch also injects the value at runtime. A field may map to multiple env vars (e.g. GCP project → GOOGLE_CLOUD_PROJECT and GOOGLE_PROJECT) by giving a list.",
          "oneOf": [
            { "type": "string" },
            { "type": "array", "items": { "type": "string" }, "minItems": 1 }
          ]
        }
      },
      "allOf": [
        {
          "if":   { "properties": { "type": { "const": "enum" } } },
          "then": { "required": ["enum"] }
        }
      ]
    },

    "windows_install_spec": {
      "description": "Windows install step specification.",
      "type": "object",
      "required": ["steps"],
      "additionalProperties": false,
      "properties": {
        "steps": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 1
        },
        "state_files": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": ["env.json"]
          }
        },
        "fallback": {
          "type": "boolean"
        }
      }
    },

    "catalog_contribution": {
      "description": "Provider catalog contribution block: machines, provider-specific OS image/compatibility rows, and inits.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "machines": {
          "type": "array",
          "items": { "$ref": "#/$defs/catalog_machine" }
        },
        "oses": {
          "type": "array",
          "items": { "$ref": "#/$defs/catalog_os" }
        },
        "inits": {
          "type": "array",
          "items": { "$ref": "#/$defs/catalog_init" }
        }
      }
    },

    "catalog_machine": {
      "description": "A machine row contributed by a provider manifest, mirroring config/catalog.yaml machine shapes.",
      "type": "object",
      "required": ["name", "kind"],
      "additionalProperties": true,
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "provider": { "type": "string" },
        "kind": { "type": "string", "enum": ["vm", "metal"] },
        "supports": { "type": "object", "additionalProperties": true },
        "defaults": { "type": "object", "additionalProperties": true }
      }
    },

    "catalog_os": {
      "description": "A provider-specific OS image/compatibility contribution. Only the provider's own fields are declared here (e.g. aws_ami_name_pattern, vultr_os_id, cloud_image_url); shared identity (family/version/arch) is merged by the aggregator.",
      "type": "object",
      "required": ["id"],
      "additionalProperties": true,
      "properties": {
        "id": { "type": "string", "minLength": 1, "description": "OS identifier matching a shared OS identity row." },
        "family": { "type": "string" },
        "version": { "type": "string" },
        "arch": { "type": "string", "enum": ["amd64", "arm64"] },
        "vagrant_box": { "type": "string" },
        "cloud_image_url": { "type": "string" },
        "cloud_image_sha256": { "type": "string" },
        "aws_ami_name_pattern": { "type": "string" },
        "gcp_image_project": { "type": "string" },
        "gcp_image_family": { "type": "string" },
        "vultr_os_id": { "type": ["string", "integer"] },
        "metal_image": { "type": "string" }
      }
    },

    "catalog_init": {
      "description": "An init row contributed by a provider manifest, mirroring config/catalog.yaml init shapes.",
      "type": "object",
      "required": ["id"],
      "additionalProperties": true,
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "os_family": { "type": "string" },
        "providers": { "type": "array", "items": { "type": "string" } }
      }
    },

    "bundle_entry": {
      "description": "A bundle composes a set of included package ids.",
      "type": "object",
      "required": ["id", "includes"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "pattern": "^[a-z][a-z0-9-]*$" },
        "includes": {
          "type": "array",
          "items": { "type": "string" },
          "minItems": 1,
          "uniqueItems": true
        }
      }
    },

    "artifact_entry": {
      "description": "A declared build output for a package. Phase 0: declaration only — no execution wiring.",
      "type": "object",
      "required": ["id"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "path": { "type": "string", "minLength": 1 },
        "download": {
          "type": "string",
          "minLength": 1,
          "description": "Action target for downloading this artifact."
        }
      }
    }
  }
}
