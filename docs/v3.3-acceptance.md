# v3.3 Acceptance Verification

Use this as the PR acceptance section.

| Item | Status | Evidence |
| --- | --- | --- |
| `make test` green on fresh Ubuntu 24.04 with apt-only deps | PASS | Fresh `ubuntu:24.04` container installed apt deps, Terraform 1.15.4 from HashiCorp apt, Terramate 0.17.0 release tarball, Bundler 4.0.11, Poetry 2.4.1; `make doctor` and `make test` both passed. |
| Docker slim image builds and runs Eve | PASS | Rebuild + smoke was performed twice: original verification and the Section 4 follow-up. `make docker.runtime.slim` succeeded; `docker run --rm eve/eve:slim make help` exited 0; `docker run -it -v ~/.eve-data:/data eve/eve:slim make eve` rendered the Eve first-run TUI. |
| `eve/eve:full` builds and runs | PASS | `make docker.runtime.full` succeeded; `docker run --rm --platform linux/amd64 eve/eve:full make help` exited 0. |
| Docker slim image size | PASS | `docker image inspect eve/eve:slim --format '{{.Size}}'` reported `577610693` bytes, under 700 MB. |
| Docker runtime has usable repo root | PASS | `docker run --rm -v ~/.eve-data:/data eve/eve:slim make providers.status` ran without missing `.git` errors. |
| Docker can drive at least one cloud provider end-to-end | BLOCKED | Deferred to user (post-Section 1 fix). The TUI Settings/ProviderConfig editing regression (fixed in Section 1) blocked this acceptance item from being tested by the agent. User will run this manually: configure Vultr API key via Settings in `eve/eve:slim`, then `make eve` create/provision/ssh/delete an instance, and update this row to PASS with command evidence. |
| README has Ubuntu and Docker quickstarts | PASS | `README.md` contains `Quickstart on Ubuntu` and `Quickstart with Docker`, with copy-pasteable command blocks. |
| CI runs macOS and Ubuntu | PENDING | Workflow matrix is configured for `macos-latest` and `ubuntu-24.04`; GitHub CI must run after the branch is pushed and PR is opened. |

Branch protection follow-up: make the Ubuntu matrix job required after the PR check name is visible in GitHub.
