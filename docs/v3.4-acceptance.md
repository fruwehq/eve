# v3.4 Acceptance Evidence

Evidence captured on 2026-05-26 from branch
`v3.4-ci-perf-python-migration`.

| Acceptance item | Status | Evidence |
| --- | --- | --- |
| Clean PR run completes required checks in less than 15 minutes, excluding queue time | FAIL | PR #26 CI run 26424961705 completed `test (macos-latest)` in 12m1s, `test (ubuntu-24.04)` in 23m19s, and `docker-runtime-slim` in 3m23s. Root cause is `test-plugins` + `test-instances` subprocess overhead; optimization is deferred to v3.5. |
| Required PR checks are unchanged | PASS | `.github/workflows/test.yml` still defines required PR jobs `test (macos-latest)`, `test (ubuntu-24.04)`, and `docker-runtime-slim`; `docker-runtime-full` remains schedule-only. `poetry run python scripts/test-ci-pipeline --offline` passed after C.9 and asserts this contract. |
| `docker-runtime-slim` builds once, smokes once, reports compressed size, and enforces the size threshold | PASS | `scripts/test-ci-pipeline` asserts exactly one slim Docker build, a single `make help` smoke step, compressed and uncompressed size reporting, and active caps of 500 MB compressed / 1600 MB uncompressed. Final local `docker build -f docker/Dockerfile --target runtime-slim -t eve/eve:slim .` and `docker run --rm eve/eve:slim make help` passed; local ARM64 slim size was 283,589,105 bytes inspect and 281,804,634 bytes gzipped. |
| Docker runtime no longer compiles Ruby or Python from source | PASS | `docker/Dockerfile` uses `python:3.14-slim-trixie`; the CI pipeline validator asserts there is no Python source download. Ruby build/install stages were removed in C.8. |
| Slim runtime no longer contains Ruby | PASS | C.8 verification built `eve/eve:slim` and `docker run --rm eve/eve:slim sh -c 'command -v ruby || echo no-ruby'` printed `no-ruby`. Final C.10 slim rebuild repeated the check and also printed `no-ruby`; `find . -name '*.rb' -not -path './.git/*' -not -path './node_modules/*'` returned no files, and the CI/Docker/Make grep for Ruby/Bundler/Gemfile references returned no matches. |
| `make test` is green on macOS and Ubuntu | PASS | macOS local `make test` passed after every v3.4 commit through C.10. PR #26 CI run 26424961705 passed `test (macos-latest)` in 12m1s and `test (ubuntu-24.04)` in 23m19s. |
| `make test.ci-pipeline` verifies the optimized workflow contract | PASS | `poetry run python scripts/test-ci-pipeline --offline` passed after C.9. Earlier C.1 validation ran `make test.ci-pipeline` with remote Terramate release checks before later local sandbox DNS restrictions made offline validation the reliable local mode. |
| Full runtime image remains schedule-only and records size/build evidence | PASS | `docker-runtime-full` remains guarded by `if: github.event_name == 'schedule'`, now uses Buildx/GHA cache, runs `docker run --rm --platform linux/amd64 eve/eve:full make help`, and reports size. Local cold `linux/amd64` full build took 170.29s; inspect size was 452,479,304 bytes and gzipped `docker save` size was 449,203,776 bytes. `qemu-system-aarch64`, `qemu-system-x86_64`, and `vagrant` were present. |
| `qemu-system-aarch64` placement was measured | PASS | Inside `eve/eve:full`, installed sizes were `vagrant` 267,447 KiB, `qemu-system-x86` 64,314 KiB, `qemu-system-arm` 63,814 KiB, `qemu-system-data` 18,478 KiB, and `qemu-system-common` 4,157 KiB. Because `qemu-system-arm` supplies `qemu-system-aarch64` at roughly the same footprint as x86 QEMU and `eve/eve:full` is explicitly the local QEMU/Vagrant variant, it stays in `runtime-full` instead of splitting a new image. |
