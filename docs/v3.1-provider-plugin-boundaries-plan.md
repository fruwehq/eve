# v3.1 Plan: Provider Boundaries and TUI Control Surface

## Summary

v3 made instances, provider plugins, package plugins, and state management real.
v3.1 should finish the separation: provider-specific implementation belongs in
provider plugins, package-specific implementation belongs in package plugins, and
core should stay a provider-agnostic orchestrator.

This also affects the TUI. The TUI should show provider health and expose
provider/package actions without hardcoding AWS, GCP, TrueNAS, Vultr, QEMU,
Raspberry Pi, Windows, or Ubuntu behavior.

## Implementation Status

This branch completes the first v3.1 slices:

- observed-state cache in `.egame/state/instances/<name>.json`
- `make instance.observe` for non-mutating live status/IP refresh
- provider manifests route through plugin-local command wrappers
- TUI provider-health display, observed-state refresh, lifecycle/debug/package
  controls, upload chooser, and expanded busy output
- selected upload folders through `make upload INSTANCE=<name> UPLOADS=a,b`
- canonical `VM_USER_NAME` for managed Linux guests, with image-default user
  fallback removed from normal orchestration paths
- built-in Terraform/Terramate provider stacks/modules relocated into
  `plugins/providers/{aws,gcp,truenas,vultr}/`
- `all` Makefile target removed

Still intentionally deferred: moving local-QEMU and Raspberry Pi provider
implementation fully behind plugin-local command directories. The wrapper
commands create the manifest boundary now; the non-Terraform providers still
need the same physical relocation and contract tightening.

## Design Direction

- Core owns:
  - catalog loading and instance resolution
  - plugin discovery and manifest validation
  - instance state files and operation history
  - shared support helpers such as per-instance workdir/state path creation
  - generic dispatch to provider/package commands
  - TUI rendering and command streaming
- Provider plugins own:
  - provider lifecycle implementation: `resolve`, `init`, `plan`, `up`, `down`,
    `start`, `stop`, `status`, `ip`, `ssh`
  - provider-specific Terraform/Terramate/Vagrant/metal files
  - provider-specific credential checks and login/admin actions where possible
  - provider-specific password retrieval behavior
- Package plugins own:
  - package install/status/down/reinstall implementation
  - package-specific host-side actions shown in the TUI
  - package-specific upload/config files when needed
  - package-specific provisioning steps, service files, templates, and remote
    action helpers
  - package dependencies and package-specific observed state

`init` and `plan` are provider plugin command contract entries only. They are
not top-level `make` targets, because Eve should request normal lifecycle goals
like `up` and let the provider decide which internal steps are needed.
Built-in provider manifests now point to plugin-local command dispatchers. AWS,
GCP, TrueNAS, and Vultr own their Terramate stacks/modules under
`plugins/providers/<id>/`. `scripts/provider-plugin` is retained only as an old
compatibility shim and should not be used by built-in provider manifests.

## Provider Plugin Layout

Target built-in provider layout:

```text
plugins/providers/aws/
  egame-plugin.yaml
  commands/
    init
    plan
    up
    down
    start
    stop
    status
    ip
    ssh
    password
    login
  stacks/
  modules/

plugins/providers/local-qemu/
  egame-plugin.yaml
  commands/
    init
    plan
    up
    down
    start
    stop
    status
    ip
    ssh
  vagrant/
    Vagrantfile template/support
```

Core may provide helper commands/functions like:

- resolve instance JSON
- create/read per-instance workdir paths
- create/read per-instance Terraform data/state paths
- stream command output
- record provider/package operation state
- validate command JSON contracts

Provider plugins should call those helpers instead of duplicating path and state
logic.

## Package Plugin Layout and Dependencies

Target built-in package layout:

```text
plugins/packages/vnc/
  egame-plugin.yaml
  provision/
    ubuntu/
      install.sh
      templates/
  commands/
    ubuntu/
      status
      down
      vnc

plugins/packages/gnome-desktop/
  egame-plugin.yaml
  provision/
    ubuntu/
      install.sh
```

Package-specific files should move out of `linux/provision/scripts/steps/` and
`windows/provision/scripts/steps/`. Shared primitives can stay in core only when
they are truly generic, for example:

- apt/dpkg lock handling
- download/cache helpers
- state/log helpers
- service-runner helpers
- package graph sorting

Current v3.1 migration state:

- Linux package manifests can reference shared core step filenames such as
  `base.sh` or plugin-local paths such as `provision/ubuntu/vnc.sh`.
- `make provision` and `make package.install` stage those plugin-local steps
  into the remote runner before execution.
- Built-in Linux package-specific steps have moved to
  `plugins/packages/<id>/provision/ubuntu/`; the central Linux step directory
  now only contains shared runner steps.
- Granular Windows package installs can also reference plugin-local paths such
  as `provision/windows/rustdesk.ps1`. The package installer copies those
  steps into its temporary remote runner directory, while full Windows
  provisioning stages selected plugin-local Windows steps before upload.
- Built-in granular Windows package steps for Steam, RustDesk, and Xpra have
  moved to `plugins/packages/<id>/provision/windows/`. Larger reboot-coupled
  Windows primitives such as Sunshine, VDD, ViGEmBus, and GPU setup still run
  through the full Windows provisioning state machine.

Package manifests should describe dependencies instead of relying on numeric
filename order:

```yaml
depends_on:
  any_of:
    - [gnome-desktop]
    - [kde-desktop]
  all_of:
    - base
    - timezone
```

Resolution rules:

- `all_of` dependencies are always included before the package.
- `any_of` means one provider of the capability is required; if the user already
  selected one, use it, otherwise choose the catalog default for that OS/provider.
- Dependencies are resolved into a DAG and topologically sorted.
- Cycles and missing providers are validation errors before any VM mutation.
- Bundles select packages only; install order comes from the dependency graph.

This would let `vnc` depend on a desktop capability without caring whether the
selected desktop is GNOME, KDE, XFCE, or a future plugin. It also removes hidden
meaning from names such as `vnc.sh`.

## Provider and Package Actions

Provider and package plugins should expose optional UI actions through manifest
metadata. Core should render these actions when their declared state predicate is
true, without hardcoding provider/package names in the TUI.

Provider action examples:

- Terraform/Terramate providers expose `init`, `plan`, and `status details` as
  debug actions.
- TrueNAS can expose host/control diagnostics.
- AWS/GCP/Vultr can expose login/auth or quota checks when useful.

Package action examples:

- Sunshine exposes `Sunshine`, `Pair Moonlight`, and `Moonlight`.
- VNC exposes `VNC`, with a viewer choice prompt.
- Waypipe exposes a Wayland test action only when local prerequisites are
  detected.

Action metadata should support:

- label and help text
- command target
- optional prompt choices
- required provider/guest/package state
- destructive flag
- expected output shape

Custom state should follow the same pattern:

- provider plugins emit normalized provider state plus provider-specific details
- package plugins emit normalized package state plus package-specific details
- core stores both in observed state and renders normalized state by default

## VM Identity and Init Normalization

v3.1 should remove implicit fallback users from core paths. Today some Linux
paths still fall back to image defaults such as `ubuntu`, and Windows naturally
starts from `Administrator`. That is useful while migrating old instances, but
it keeps provider/OS assumptions in shared scripts.

Target behavior:

- Providers own bootstrap-user negotiation. Core must not assume that
  `VM_USER_NAME` is reachable first.
- `EGAME_PROVISION_USER` is the persistent automation user for unattended
  `egame` commands. It should be key-only where the provider/OS supports that.
- `VM_USER_NAME` is the canonical human/admin VM username for Linux and other
  Unix-like guests.
- `EPHEMERAL_WINDOWS_PASSWORD` / provider password retrieval are provider/OS
  capabilities, not global assumptions.
- Provider/OS init is responsible for creating, renaming, or enabling the
  provision user and human user before later package/provisioning steps run.
- If a base image has a preprogrammed user (`ubuntu`, `Administrator`, etc.),
  provider init should use that account only as a bootstrap path, then normalize
  the instance by creating or repairing the configured provision and human
  accounts.
- Core unattended SSH/IP/status helpers should use the resolved provision user
  after init normalization. Interactive/manual connection helpers can expose the
  human user when appropriate.
- Compatibility fallbacks to image-default users should be removed once existing
  v3 instances are either recreated or migrated.
- Live migration should be explicit: add a one-shot migration/debug command if
  we need to repair an old VM, rather than keeping permanent fallback logic in
  normal operation.

This keeps new instances provider-agnostic: after init, package plugins should
not care whether the VM came from TrueNAS cloud-init, GCP, AWS, Vultr, QEMU, or
metal Raspberry Pi.

Recommended security model:

- Bootstrap user: provider/image-owned first reachable account. It may be
  static (`vagrant`, `Administrator`, an existing Raspberry Pi user), derived
  from cloud metadata, or absent when provider init can create users directly.
- Provision user: persistent key-only automation account used by scripts. It is
  allowed passwordless sudo/admin because unattended package install, reboot,
  service management, and remote-access repair require privileged commands.
- Human user: password-enabled account exposed to the actual VM user. It should
  have admin rights, but core automation should not depend on password prompts.
- Raspberry Pi: treat the existing reachable Pi login as the provision user and
  create/use a separate `VM_USER_NAME` human account. Do not delete or rename
  the adopted Pi account automatically.
- Windows: bootstrap through `Administrator` when that is what the provider
  exposes, then normalize toward `VM_USER_NAME` as the human admin account.

The implementation should model these identities explicitly, for example:

```json
{
  "access": {
    "bootstrap_user": "ubuntu",
    "provision_user": "egame-provision",
    "human_user": "chris"
  }
}
```

Provider-specific rules decide when `bootstrap_user` and `provision_user` are
the same account. Core scripts should call provider access helpers instead of
hardcoding those rules.

This preserves unattended `egame` commands while giving the user a normal
password-enabled admin account on every Linux and Windows guest.

Migration caveat: many current Linux provision steps use `$USER` for desktop
session files, RustDesk config, VNC home files, autologin, and user services.
After `provision_user` and `human_user` diverge, package steps must receive the
human user explicitly and write user-scoped desktop/session config for that
account, while still running privileged commands through the provision user.

## Structured Configuration

The `.env` file is still useful for secrets and host-local overrides, but v3.1
should add a structured config file for non-secret user preferences and defaults:

```yaml
defaults:
  location: tokyo
  display:
    resolution: 5120x1440
    fps: 60
  vm_user_name: chris

providers:
  truenas:
    host: 192.168.0.52
    ssh_port: 22

tui:
  refresh_interval_seconds: 5
```

Proposed precedence:

1. command-line overrides
2. instance registry values
3. `.egame/config.yaml`
4. `.env.local`
5. catalog defaults

Secrets should stay in `.env.local` or a future secret store. The TUI can safely
edit `.egame/config.yaml`, which would let users change global display
resolution and similar preferences without editing shell env files.

## Provider State Model

Keep the semantic split:

- `up`: register/create provider resources. Disable once resources exist.
- `start`: power on an existing stopped VM. Disable when running.
- `stop`: power off an existing running VM. Disable when stopped/absent.
- `down`: deregister/destroy provider resources. This is the inverse of `up`.
- `delete`: remove the local instance registry entry. This is not a provider
  lifecycle command.

Rename TUI wording:

- Replace the current dangerous `Destroy` label with `Down` or a clearer pair:
  `Create` / `Destroy Provider Resource`.
- Avoid showing `Down` and `Delete` as peer buttons without explanation.
- Put `Down` in a Provider Debug/Danger area.
- Put `Delete local entry` below it with distinct copy explaining it only removes
  the local registry entry and does not destroy provider resources.

## Observed State Cache

v3 currently has local lifecycle state, but not a complete observed-state cache.
`.egame/state/instances/<name>.json` stores desired/provider/provision/package
state and operation history. The TUI also keeps in-memory status snapshots and
refreshes selected/live provider status in the background. IP addresses,
provider reachability details, remote display readiness, and package action
availability are still partly recomputed on demand.

v3.1 should make this explicit and smoother:

- Extend instance state with an `observed_state` object owned by core:
  - `provider_status`: normalized live provider status such as `running`,
    `stopped`, `absent`, `unreachable`, or `unknown`
  - `provider_status_raw`: provider-specific status payload or short summary
  - `ip`: last known IP address, with source and timestamp
  - `control_reachable`: whether the provider control path was reachable
  - `guest_reachable`: whether SSH/RDP/etc. guest access was reachable
  - `observed_at` and `expires_at`
  - `refresh_error`: last observation error, separate from lifecycle errors
- Keep `desired_state` and `provider_state` as orchestration intent/state, not
  as the only UI truth.
- On TUI startup:
  - render instances immediately from local registry plus cached observed state
  - mark stale fields visually as refreshing
  - refresh observed state asynchronously without moving focus or blocking input
  - replace stale values only when the refresh belongs to the still-selected row
- Provider `status` and `ip` commands should return machine-readable JSON so the
  core cache can update `observed_state` without parsing provider text output.
- Cache refresh should be rate-limited and cancellable; failed refreshes should
  not erase a recent known-good stopped/absent state.
- Package action availability should be derived from selected packages,
  package-state cache, and live observed provider/guest reachability.

This gives the user a fast, stable UI: stale data is clearly marked, but the TUI
does not jump, block, or show contradictory state while background checks run.

## TUI v3.1 Changes

### Provider Status Area

Add a provider-status area at the bottom of the upper-left panel:

- Run provider status once at TUI startup.
- Show each provider with configured/reachable state.
- Do not poll provider health constantly by default.
- Clicking a provider opens a provider menu.

Provider menu candidates:

- status/details
- doctor/provider check
- login/auth action if the provider plugin exposes one
- direct provider-specific admin action, such as TrueNAS host SSH, if exposed by
  the provider plugin

This should replace provider-specific Makefile concepts like `aws.login` and
`ssh.truenas` over time. Those actions should become provider plugin actions,
not hardcoded core/TUI behavior.

### Instance Lifecycle Area

Expose state-aware provider lifecycle controls:

- `Up`: visible/enabled only when provider resources are absent or unregistered.
- `Start`: visible/enabled only when provider resources exist and are stopped.
- `Stop`: visible/enabled only when provider resources are running.
- `Plan`: provider debug action exposed by provider metadata/dispatch, not a
  core Makefile action.
- `Init`: provider debug action exposed by provider metadata/dispatch, not a
  core Makefile action.
- `Provider Status`: secondary action, also useful after failed operations.
- `Reboot`: visible/enabled when provider is running, with confirmation.
- `Update`: visible/enabled when provider is running, with confirmation.
- `Show Password`: visible when the selected provider/OS exposes a password
  command or password capability. This is provider and OS dependent, not strictly
  Windows-only.

### Debug Panel

Add a collapsible or visually separated debug panel above provider down/delete:

- `Provider Status`
- `Plan`
- `Init`
- `Provision Wait`
- `Clear Provision State`
- `Restart Provision`
- `Recover Interrupted Operation`
- `Upload...`

Every destructive or state-resetting action needs confirmation.

`instance.recover` means: if local state says the last operation is still
`running` because the host process/TUI was interrupted, mark that operation
`failed`, set provider state to `error`, and record a recovery error. It does
not reach into the VM or provider; it only repairs local orchestration state so
the user can retry deliberately.

### Package Area

Add:

- `Reinstall` next to Install/Uninstall with destructive confirmation if needed.
- Clearer package lock/source labels:
  - bundle-selected packages are selected and locked by their bundle
  - directly selected packages can be removed
  - unsupported packages are disabled with reason

### Upload UX

Replace the coarse `upload` action with a TUI upload chooser:

- List direct subfolders under `upload/`.
- Let the user choose one or more subfolders.
- Upload selected subfolders to both Linux and Windows instances through
  provider/package-agnostic instance upload plumbing.
- The Makefile/script surface should accept selected upload names rather than
  blindly uploading every file under `upload/`.

## Makefile Cleanup

Remove:

- `all`

Reason: it hides important lifecycle steps (`init`, `up`, `ssh.wait`,
`provision`, `provision.wait`) behind one coarse target. The TUI should expose
and stream each step explicitly.

Eventually convert provider-specific host tasks into provider plugin actions:

- `aws.login`
- `ssh.truenas`

Keep CLI-only:

- `ssh.run`: future TUI command modal only if needed.
- test/lint/golden/dev maintenance targets.

## PROFILE Compatibility Naming Cleanup

v3 removed user-facing recipes/profiles, but some internal compatibility paths
still use `PROFILE` or `profile` as a generic overlay/target name. v3.1 should
rename those internal variables, log messages, and helper arguments where it is
safe to do so:

- Prefer names such as `INSTANCE_NAME`, `TARGET_NAME`, or `OVERLAY_NAME` when
  the value is a concrete instance or generated instance overlay.
- Keep `PROFILE` only at explicit legacy compatibility boundaries while those
  boundaries still exist.
- Update error messages and help text so user-facing commands consistently
  refer to `INSTANCE=`.
- Remove the compatibility naming entirely once old profile-oriented scripts no
  longer need to accept legacy `PROFILE=` entry points.

## Provider Migration Order

1. Add provider plugin action metadata for provider-level actions.
2. Add core helper scripts for per-instance paths/state/env so plugins can call
   them without reaching into central provider internals.
3. Move `local-qemu` implementation into `plugins/providers/local-qemu/`.
   This is the smallest provider and a good proof of the new shape.
4. Move Terraform/Terramate providers (`aws`, `gcp`, `truenas`, `vultr`) into
   their provider plugin directories. Done.
5. Move any remaining provider-specific host helpers for those providers behind
   plugin-local commands instead of compatibility dispatch. Done for the main
   provider command dispatcher; deeper helper ownership can continue per
   provider as needed.
6. Move `raspberry-pi` metal implementation into its provider plugin directory.
7. Remove `scripts/provider-plugin` after any external or historical callers are
   migrated.
8. Update tests to assert provider implementations no longer reference central
   provider-specific stack/module paths.

## Test Plan

- Unit/script tests:
  - provider manifests validate action metadata
  - provider dispatch can execute plugin-local commands
  - provider action list appears in TUI data sources
  - `instance.recover` remains local-state-only
  - upload selection rejects path traversal and missing folders
- Regression tests:
  - `make test`
  - provider dry-run matrix for every built-in provider
  - package action and reinstall dispatch
  - TUI `--check`
- Live smoke tests:
  - local-qemu Ubuntu baseline
  - one Terraform Ubuntu provider baseline, preferably GCP or TrueNAS
  - Vultr Windows baseline for password/show-password and remote GUI actions
  - optional `integration.packages` after baseline is stable

## Open Decisions

- TUI wording for provider resource deletion:
  - `Down` matches Makefile/provider contract.
  - `Destroy Provider Resource` is clearer but longer.
  - `Delete Local Entry` should replace plain `Delete` in dangerous UI sections.
- Whether provider actions should be a new manifest top-level key, e.g.
  `actions:`, or an extension under `commands:`.
- Whether `show-password` should become a standard provider command such as
  `password`, or a provider action only exposed when supported.
