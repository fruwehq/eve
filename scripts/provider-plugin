#!/usr/bin/env ruby
# Built-in provider plugin compatibility wrapper around existing profile scripts.

require "json"

ROOT = File.expand_path("..", __dir__)
provider = ARGV.shift
command = ARGV.shift
extra_args = ARGV

unless provider && command
  warn "Usage: scripts/provider-plugin <provider> <command> [args...]"
  exit 2
end

json_source = ENV["EGAME_RESOLVED_JSON"]
resolved = if json_source
             JSON.parse(json_source)
           else
             JSON.parse($stdin.read)
           end
actual_provider = resolved.fetch("machine").fetch("provider")
profile = resolved.fetch("instance").fetch("name")

if actual_provider != provider
  warn "provider-plugin: manifest provider #{provider} does not match resolved provider #{actual_provider}"
  exit 1
end

if ENV["EGAME_PLUGIN_DRY_RUN"] == "1" || command == "resolve"
  puts JSON.generate({
    "kind" => "provider",
    "provider" => provider,
    "command" => command,
    "profile" => profile,
    "engine" => resolved.fetch("engine"),
    "dry_run" => ENV["EGAME_PLUGIN_DRY_RUN"] == "1"
  })
  exit 0
end

make_targets = {
  "start" => "start",
  "stop" => "stop",
  "status" => "status"
}

case command
when "init"
  if resolved.fetch("engine") == "metal"
    warn "[init] instance=#{profile} uses metal hardware; no Terraform backend to initialize."
    exit 0
  end
  exec File.join(ROOT, "scripts/tf-init"), profile
when "plan"
  case resolved.fetch("engine")
  when "vagrant"
    exec File.join(ROOT, "scripts/vagrant-up"), "--plan", profile
  when "metal"
    warn "[plan] instance=#{profile} uses metal hardware; provisioning will run over SSH."
    exec File.join(ROOT, "scripts/profile-resolve"), "--profile", profile, "--emit", "env"
  else
    exec File.join(ROOT, "scripts/tf-plan"), profile
  end
when "up"
  case resolved.fetch("engine")
  when "vagrant"
    exec File.join(ROOT, "scripts/vagrant-up"), profile
  when "metal"
    warn "[up] instance=#{profile} uses metal hardware; nothing to create."
    exit 0
  else
    exec File.join(ROOT, "scripts/tf-apply"), profile
  end
when "down"
  case resolved.fetch("engine")
  when "vagrant"
    exec File.join(ROOT, "scripts/vagrant-destroy"), profile
  when "metal"
    warn "[down] instance=#{profile} uses metal hardware; nothing to destroy."
    exit 0
  else
    exec File.join(ROOT, "scripts/tf-destroy"), profile
  end
when *make_targets.keys
  exec File.join(ROOT, "scripts", make_targets.fetch(command)), profile
when "ip"
  exec File.join(ROOT, "scripts/instance-ip"), profile
when "ssh"
  exec File.join(ROOT, "scripts/instance-ssh"), profile, *extra_args
else
  warn "provider-plugin: unsupported command: #{command}"
  exit 2
end
