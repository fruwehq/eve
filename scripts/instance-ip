#!/usr/bin/env ruby
# frozen_string_literal: true

ROOT = File.expand_path("..", __dir__)
instance = ARGV.shift || ENV["INSTANCE"]

unless instance && !instance.empty?
  warn "Usage: scripts/instance-ip <instance>"
  exit 2
end

exec(File.join(ROOT, "scripts/provider-dispatch"), "--instance", instance, "--command", "ip")
